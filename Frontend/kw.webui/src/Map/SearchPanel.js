import { Typography } from "@mui/material";

const SearchPanel = () => {
    return(
    <span id='SearchPanel'>
        <Typography
            variant="h5"
            textAlign={"center"}>
                Panel wyszukiwania
            </Typography>
    </span>)
}

export default SearchPanel;